[pytest]
testpaths = tests
addopts = -ra --strict-config --strict-markers
filterwarnings =
    error::pytest.PytestConfigWarning
markers =
    functional: end-to-end application workflow tests
    security: security regression tests
    rbac: role and tenant isolation tests
    smoke: application process and HTTP startup tests
    ui: rendered page and frontend contract tests
